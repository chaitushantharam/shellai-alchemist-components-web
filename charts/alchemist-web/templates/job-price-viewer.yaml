apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: price-viewer
  labels:
    ingestion: price-viewer
spec:
  schedule: "0 3 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          imagePullSecrets:
            - name: shellai-secret
          containers:
          - name: price-viewer
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
            imagePullPolicy: {{ .Values.image.pullPolicy }}
            args:
              - price_viewer
            envFrom:
              - secretRef:
                  name: alchemist-web
              - configMapRef:
                  name: alchemist-web
              - configMapRef:
                  name: alchemist-jobs
              resources:
                limits:
                  cpu: "{{ STATTEST_CPU_LIMITS }}"
                  memory: "{{ STATTEST_MEMORY_LIMITS }}"
                requests:
                  cpu: "{{ STATTEST_CPU_REQUESTS }}"
                  memory: "{{ STATTEST_MEMORY_REQUESTS }}"
          restartPolicy: Never

